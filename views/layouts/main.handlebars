<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Social Game Library</title>

    <!-- Bootstrap stylesheet -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <base href="/">
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Game Tracker</a>

            <!-- When site shrinks to a certain point, toggle button appear to see the links -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Links -->
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    <!-- Homepage -->
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/">Home</a>
                    </li>

                    <!-- Library: See the games you added, and add new games -->
                    <li class="nav-item">
                        <a class="nav-link" href="/library">Library</a>
                    </li>

                    <!-- Social: See your friends game collection -->
                    <li class="nav-item">
                        <a class="nav-link" href="/social">Social</a>
                    </li>
                </ul>

                <!-- Separated into different ul so that we can align it to the end of navbar -->
                <ul class="nav list-inline ml-auto">

                    <!-- If the user is logged in, they have the option to log out and look at their friends list -->
                    {{#if logged_in}}

                    <!-- Friends List: Check your friends list -->
                    <!-- The friends list appears in a offcanvas -->
                    <li class="nav-item">
                        <a class="nav-link" data-bs-toggle="offcanvas" href="#offcanvasExample" role="button"
                            aria-controls="offcanvasExample">Friends List</a>
                        {{!-- <a class="btn btn-primary" data-bs-toggle="offcanvas" href="#offcanvasExample"
                            role="button" aria-controls="offcanvasExample">
                            Link with href
                        </a> --}}
                    </li>

                    <!-- Logout: Log out of the session -->
                    <li class="nav-item">
                        <button class="btn" onclick="logout()">Logout</a>
                    </li>

                    <!-- If the user is not logged in, they can only check the login page to sign up or login -->
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>

                    {{/if}}

                </ul>
            </div>
        </div>
    </nav>

    <!-- Content of each individual page -->
    {{{ body }}}

    <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="offcanvasExampleLabel">Friend's List</h5>
            <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        </div>
        <div class="offcanvas-body">
            <div>
                <form class="new-request-form">
                    <div class="form-group">
                        <label for="exampleInputEmail1">Friend Request To:</label>
                        <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp"
                            placeholder="Enter their user email">
                        <small id="emailHelp" class="form-text text-muted">Click below to submit friend request.</small>
                    </div>
                
                    <button type="submit" class="btn btn-primary">Submit Friend Request</button>
                </form>

                <h4 class="mb-3">Your Friends:</h4>

                <ul id = 'friendlist'>
        
                </ul>
                
                {{!-- {{#each data as |user|}}
                {{#if user.friends.length}}
                {{#each friends as |friend|}}
                <h4>{{friend.name}}</h4>
                {{/each}}
                {{/if}}
                {{/each}} --}}
            </div>
        </div>
    </div>

    <!-- Bootstrap script bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>

    <script src="/js/login.js"></script>

    {{#if logged_in}}
    <script src="/js/friends.js"></script>
    {{/if}}
</body>

</html>